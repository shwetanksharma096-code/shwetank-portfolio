/**
 * firebase.ts — STUB (Firebase has been removed from this project).
 * This file exists only so any residual import paths don't break during
 * the transition. It exports nothing functional.
 *
 * Image storage → Cloudinary
 * Data storage  → localStorage (via store.ts)
 * Campaign data → Direct Google Sheets CSV fetch
 */

export const db = null as any;
export const storage = null as any;
